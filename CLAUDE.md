# 给 Claude 的说明（homework_answer）

- **一律用中文回复。**
- 这个仓库由每天 8 点的定时任务驱动，每次运行都是全新会话、不记得上一次做过什么。
  所有进度都在仓库里：开工前先读根目录的 `进度.md`（里面有「解题规范」和各科进度）。

## 解题时必须用的 skill

1. **所有解题一律先调用 `math-cixibara` skill。** 不限科目——语文、英语、物理、数学
   都算解题；检查阶段"重新独立解一遍"同样算解题，也要先调用。这是硬要求。

2. **数学的提示阶梯，写法标准照 `math-hint` skill 里「提示怎么写」那一节：**
   - 写提示前必须自己先把题完整解完并验算，再倒推着写提示。
   - 2~4 条递进（难题可到 5 条）：第一条只点方向不给结论，最后一条到「差一步就能算
     出来」为止。
   - **任何一条提示里都不能出现最终答案**；中间量可以露，结果不能露。可以预告答案的
     「形状」（"答案是个整数""是含 √5 的分数"），帮学生自查又不算给答案。
   - 条与条之间要有真实的信息梯度，不能同一件事换个说法说两遍。
   - **输出形式是 `答案.md` 里的 `<details><summary>提示 N</summary>` 折叠块。**
     这是离线写 markdown 的批量任务，没有 Visualize 面板，不要调
     `mcp__visualize__show_widget`——只借 math-hint 的写法标准，不用它的渲染方式。

3. **`math-stepbystep` 不用。** 那是交互式分步讲解面板用的，跟写 `答案.md` 无关。

4. `math-cixibara` 里「解题前弹『工具范围』四选一」这一步，无人值守的定时运行没人能
   回答，**默认按档位②（基础 ＋ 相似/圆）**——当前数学内容是第27章二次函数、第28章
   相似形。确实非用更高档工具（三角比、向量）不可的题，先按②档尽力做，并在「完整
   解析」里注明"这里用到了超出②档的方法"。

## 目录与文件名（查看器硬依赖，一个字都不能改）

- 一份卷子＝一个文件夹：`materials/<科目>/NN-短主题/`，里面放 `01.jpg 02.jpg …` 和
  `答案.md`。
- 图片文件名**必须是纯页码**（代码取文件名里第一个数字当页码）。
- 答案文件必须叫 `答案.md`，跟图片放同一个文件夹，不能单独塞进 `answers/`。
- `答案.md` 用 `## 页码` 分段，**分段标题行只能有数字**（写成 `## 01 力的合成` 会让
  分段整个失效）。

## 答题纪律

- **看不清就说看不清**：扫描模糊、缺页、题目被裁掉的，在对应 `##` 段里写明缺什么，
  不要猜题目内容然后编答案。编出来的答案比没有答案有害得多。
- 不确定的答案要标出来，写"这题不确定，理由是…"，不要用肯定语气糊过去。
- 几何题、函数图像题、受力分析题必须配内联 SVG：用 `viewBox` 不写死宽高，线条颜色用
  `currentColor` 或深浅都看得清的颜色（查看器有深色主题）。
- 一页一提交一推送，同时更新 `进度.md`；直接推 main，不开分支、不建 PR。
- 不要改 `materials/` 以外的东西（`进度.md`、本文件除外），不要动 git 历史。
